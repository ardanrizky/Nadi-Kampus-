version: '3.8'

services:
  web:
    build: .
    container_name: nadikampus-app
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - HOST=0.0.0.0
    volumes:
      - .:/app
    restart: unless-stopped
